const { User } = require('../models');

const userData = [
    {
        username: "test",
        email: "test",
        password: "test"
    },
]

const seedUsers = () => User.bulkCreate(userData);

module.exports = userData;